import { NextResponse } from 'next/server';import { createAdminClient } from '@/lib/supabaseAdmin';import { requireAdmin } from '@/lib/adminGuard';
export async function POST(req:Request){const guard=await requireAdmin();if('error' in guard)return guard.error;const form=await req.formData();const day=Number(form.get('day'));const file=form.get('file') as File|null;if(!file)return NextResponse.json({error:'Fichier manquant'},{status:400});const buffer=Buffer.from(await file.arrayBuffer());const admin=createAdminClient();const bucket=process.env.SUPABASE_AUDIO_BUCKET||'equilibria-audios';const path=`jour-${day}.mp3`;const {error}=await admin.storage.from(bucket).upload(path,buffer,{upsert:true,contentType:file.type||'audio/mpeg'});if(error)return NextResponse.json({error:error.message},{status:500});await admin.from('chapters').update({audio_path:path}).eq('day',day);return NextResponse.json({ok:true,path})}
