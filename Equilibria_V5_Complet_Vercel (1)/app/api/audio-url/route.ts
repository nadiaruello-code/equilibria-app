import { NextResponse } from 'next/server';
import { createServerSupabaseClient, canAccessDay, Plan } from '@/lib/supabaseServer';import { createAdminClient } from '@/lib/supabaseAdmin';
export async function GET(req:Request){try{const day=Number(new URL(req.url).searchParams.get('day'));if(!day)return NextResponse.json({error:'Jour invalide'},{status:400});const supabase=createServerSupabaseClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:'Connexion requise'},{status:401});const {data:profile}=await supabase.from('profiles').select('plan').eq('id',user.id).single();const plan=(profile?.plan||'starter') as Plan;if(!canAccessDay(plan,day))return NextResponse.json({error:'Accès réservé au Voyage Complet'},{status:403});const admin=createAdminClient();const bucket=process.env.SUPABASE_AUDIO_BUCKET||'equilibria-audios';const {data,error}=await admin.storage.from(bucket).createSignedUrl(`jour-${day}.mp3`,60*20);if(error||!data?.signedUrl)return NextResponse.json({error:error?.message||'Audio introuvable'},{status:404});return NextResponse.json({url:data.signedUrl})}catch(e:any){return NextResponse.json({error:e.message},{status:500})}}
