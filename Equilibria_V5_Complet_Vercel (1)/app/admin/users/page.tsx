import { redirect } from 'next/navigation';import { createServerSupabaseClient,isAdminEmail } from '@/lib/supabaseServer';import { createAdminClient } from '@/lib/supabaseAdmin';import AdminShell from '@/components/AdminShell';import UserPlanSelect from '@/components/UserPlanSelect';
export default async function Page(){const supabase=createServerSupabaseClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login');if(!isAdminEmail(user.email))redirect('/app');const admin=createAdminClient();const {data:users}=await admin.from('profiles').select('*').order('created_at',{ascending:false});return <AdminShell><h1>Utilisateurs</h1><table><tbody>{(users||[]).map((u:any)=><tr key={u.id}><td>{u.email}</td><td>{u.plan}</td><td><UserPlanSelect userId={u.id} currentPlan={u.plan}/></td></tr>)}</tbody></table></AdminShell>}
