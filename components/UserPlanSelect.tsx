'use client';import {useState} from 'react';
export default function UserPlanSelect({userId,currentPlan}:{userId:string,currentPlan:string}){const [plan,setPlan]=useState(currentPlan);const [msg,setMsg]=useState('');async function save(){const r=await fetch('/api/admin/update-user-plan',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({userId,plan})});const d=await r.json();setMsg(d.error||'OK')}return <div className="row"><select value={plan} onChange={e=>setPlan(e.target.value)}><option value="starter">starter</option><option value="premium">premium</option><option value="circle">circle</option></select><button className="btn" onClick={save}>Changer</button><span>{msg}</span></div>}
