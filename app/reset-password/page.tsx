'use client';import {useState} from 'react';import Link from 'next/link';import {createClient} from '@/lib/supabaseBrowser';
export default function ResetPassword(){const[email,setEmail]=useState('');const[msg,setMsg]=useState('');async function reset(){const supabase=createClient();const{error}=await supabase.auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/login`});setMsg(error?error.message:'Email de réinitialisation envoyé.')}return <main className="section"><div className="container"><div className="card" style={{maxWidth:560}}><h1>Mot de passe oublié</h1><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><button className="btn gold" onClick={reset}>Réinitialiser</button><p>{msg}</p><p><Link href="/login">Retour</Link></p></div></div></main>}
