import { NextResponse } from 'next/server';import { createAdminClient } from '@/lib/supabaseAdmin';import { requireAdmin } from '@/lib/adminGuard';
export async function POST(req:Request){const guard=await requireAdmin();if('error' in guard)return guard.error;const b=await req.json();const admin=createAdminClient();const {error}=await admin.from('chapters').upsert({day:b.day,title:b.title,quote:b.quote,text_content:b.text_content,audio_path:b.audio_path},{onConflict:'day'});if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({ok:true})}
