import { redirect } from 'next/navigation';import { createServerSupabaseClient,isAdminEmail } from '@/lib/supabaseServer';import { createAdminClient } from '@/lib/supabaseAdmin';import AdminShell from '@/components/AdminShell';import ChapterEditor from '@/components/ChapterEditor';
export default async function Page(){const supabase=createServerSupabaseClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login');if(!isAdminEmail(user.email))redirect('/app');const admin=createAdminClient();const {data:chapters}=await admin.from('chapters').select('*').order('day');return <AdminShell><h1>Chapitres</h1>{(chapters||[]).map((c:any)=><ChapterEditor key={c.day} chapter={c}/>)}</AdminShell>}
