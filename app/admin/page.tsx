import { redirect } from 'next/navigation';import { createServerSupabaseClient,isAdminEmail } from '@/lib/supabaseServer';import { createAdminClient } from '@/lib/supabaseAdmin';import AdminShell from '@/components/AdminShell';
export default async function Admin(){const supabase=createServerSupabaseClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login');if(!isAdminEmail(user.email))redirect('/app');const admin=createAdminClient();const {count:users}=await admin.from('profiles').select('*',{count:'exact',head:true});const {count:chapters}=await admin.from('chapters').select('*',{count:'exact',head:true});return <AdminShell><h1>Tableau de bord</h1><div className="cards"><div className="card"><h2>{users||0}</h2><p>Utilisateurs</p></div><div className="card"><h2>{chapters||0}</h2><p>Chapitres</p></div></div></AdminShell>}
