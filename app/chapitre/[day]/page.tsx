import { redirect } from 'next/navigation';import fs from 'fs/promises';import path from 'path';import Link from 'next/link';import { createServerSupabaseClient, canAccessDay, Plan } from '@/lib/supabaseServer';import PremiumAudioPlayer from '@/components/PremiumAudioPlayer';import LumenPresence from '@/components/LumenPresence';import ChapterActions from '@/components/ChapterActions';
async function fallback(day:number){const f=await fs.readFile(path.join(process.cwd(),'public/data/chapters.json'),'utf-8');return JSON.parse(f).find((c:any)=>c.day===day)}
export default async function ChapterPage({params}:{params:{day:string}}){const day=Number(params.day);const supabase=createServerSupabaseClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login');const{data:profile}=await supabase.from('profiles').select('*').eq('id',user.id).single();const plan=String(profile?.plan||'starter').trim().toLowerCase() as Plan;if(!canAccessDay(plan,day))redirect('/offres');const{data:db}=await supabase.from('chapters').select('*').eq('day',day).single();const chapter=db?.place&&db?.symbol&&db?.quote?db:await fallback(day);if(!chapter)redirect('/app');const{data:progress}=await supabase.from('progress').select('*').eq('user_id',user.id).eq('chapter_day',day).single();return <main className="chapterExperience"><div className="chapterBackdrop"/><div className="container chapterExperienceInner"><Link className="chapterBackLink" href="/app">← Retour au voyage</Link><div className="chapterHeader"><div><p className="kicker">Chapitre {chapter.day}</p><div className="bigSymbol">{chapter.emoji||'✦'}</div><p className="chapterLocation">{chapter.place}</p><h1>{chapter.title}</h1><p className="chapterArtifact">Symbole · {chapter.symbol}</p></div><LumenPresence quote={chapter.quote}/></div><PremiumAudioPlayer day={day} title={chapter.title}/><section className="chapterReading"><p className="kicker dark">LE CHAPITRE</p><h2>Lire et intégrer</h2><p>{chapter.text_content}</p><ChapterActions day={day} initialCompleted={!!progress?.completed} initialJournal={progress?.journal||''}/></section></div></main>}
